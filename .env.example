# Copy this file to .env and fill in your keys
# cp .env.example .env

# ── Extraction provider ────────────────────────────────────────────────────────
# "xai"       — Grok 4.1 Fast ($0.20/$0.50 per MTok) — default, fastest
# "google"    — Gemini 3 Flash ($0.50/$3.00 per MTok) — native JSON mode
# "anthropic" — Claude Haiku 4.5 ($1.00/$5.00 per MTok) — Messages API
EXTRACTION_PROVIDER=xai

# Required: xAI API key — used for memory extraction (grok-4-1-fast-non-reasoning)
XAI_API_KEY=xai-...

# Optional: Google API key — required when EXTRACTION_PROVIDER=google
# Get from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional: Anthropic API key — required when EXTRACTION_PROVIDER=anthropic
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Required: Voyage AI key — used for code embeddings (voyage-code-3)
VOYAGE_API_KEY=pa-...
